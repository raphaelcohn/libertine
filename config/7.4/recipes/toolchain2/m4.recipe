set -o errexit +o hashall

version 1.4.16
{
	depends gzip
	
	include_once_per_version
}

function installPackage_toolchain2_m4()
{
	sed -i -e '/gets is a/d' lib/stdio.in.h
	
	installPackagePrototype
}
