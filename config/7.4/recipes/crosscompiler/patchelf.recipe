set -o errexit +o hashall

version 02c4ffa5550a3e35f62ae0bfed4c08812e66bf24
{
	depends musl-cross
	
	install_binaries_in_paths bin
}

function install_crosscompiler_patchelf()
{
	pushdDependencyFolder
	
		# relies on autoreconf
		./bootstrap.sh
		
	popdDepdendencyFolder

	LFS_CFLAGS_PRE="$LFS_LDFLAGS_EXTERNAL" LFS_CXXFLAGS_PRE="$LFS_LDFLAGS_EXTERNAL" configurePackagePrototype external-crosscompiler-gcc external-crosscompiler-g++ --disable-dependency-tracking
	
	LFS_CFLAGS_PRE="-v $LFS_LDFLAGS_EXTERNAL $LFS_LDFLAGS_STATIC" LFS_CXXFLAGS_PRE="-v $LFS_LDFLAGS_EXTERNAL $LFS_LDFLAGS_STATIC" makePackagePrototype external-crosscompiler-gcc external-crosscompiler-g++
}
