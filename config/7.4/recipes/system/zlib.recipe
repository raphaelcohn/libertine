set -o errexit +o hashall

version 1.2.8
{
	source_package ZLIB "$LFS_SOURCE_PACKAGE_NAME"-"${LFS_SOURCE_PACKAGE_VERSION["${LFS_SOURCE_PACKAGE_NAME}"]}".tar.xz MD5:28f1205d8dd2001f26fec1e8c2cebe37
	
	depends linux
}

function installPackage_system_zlib()
{
	installPackagePrototype --prefix=/usr
	
	mv -v /usr/lib/libz.so.* /lib
	ln -sfv ../../lib/libz.so.1.2.8 /usr/lib/libz.so
}
